#include "facecheck.h"
#include <QDebug>
FaceCheck::FaceCheck(QWidget * parent) : QWidget(parent)
{

}


void FaceCheck::work(){
    if(fileMode==1) ;
    else if(fileMode==2) ;
    else if(fileMode==3) ;
}
